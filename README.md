## Clone repository

## Make sure docker is installed

## Run this docker command:

docker-compose up -d --build

watch progress: docker-compose logs -f

once images fully pulled then:

## this will take some time since it will be downloading llam3.2 , and mistral AI model images

Go to:

http://localhost:3000

or open docker desktop and go to containers then find chat container.
under chat container you will see 3 running containers
click on web this will open your app at http://localhost:3000
